{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsNestAuth",
  "title": "Nest Auth Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the auth module.",
      "default": "auth",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the auth module (e.g., \"auth\")?"
    },
    "method": {
      "type": "string",
      "description": "The authentication method.",
      "default": "jwt",
      "enum": ["jwt", "code"],
      "x-prompt": {
        "message": "Which authentication method do you want to use?",
        "type": "list",
        "items": [
          { "value": "jwt", "label": "JWT password (Passport + access token)" },
          {
            "value": "code",
            "label": "Login code (sent by your email/SMS module)"
          }
        ]
      }
    },
    "usernameField": {
      "type": "string",
      "description": "The user field that identifies the account at login.",
      "default": "email",
      "x-prompt": "Which user field identifies the account at login? (email by default)"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the auth module."
    },
    "sourceRoot": {
      "type": "string",
      "description": "Nest auth source root directory."
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "Flag to indicate if a directory is created."
    },
    "spec": {
      "type": "boolean",
      "default": true,
      "description": "Specifies if specs file are generated."
    },
    "specFileSuffix": {
      "type": "string",
      "default": "spec",
      "description": "Specifies the file suffix of spec files."
    },
    "skipImport": {
      "type": "boolean",
      "description": "Flag to skip the module import.",
      "default": false
    },
    "format": {
      "type": "boolean",
      "default": false,
      "description": "Format generated files using Prettier if available."
    }
  }
}
